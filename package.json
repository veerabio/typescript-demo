{
  "name": "typescript-demo",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/veerabio/typescript-demo.git",
  "author": "veera <veera_cheedepudi-reddy@bio-rad.com>",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && mkdir ./dist && tsc",
    "build1": "yarn clean && mkdir ./dist && tsc --p ./src/1-hello-world/tsconfig.json",
    "build2": "yarn clean && mkdir ./dist && tsc --p ./src/2-basic-types/tsconfig.json",
    "build3": "yarn clean && mkdir ./dist && tsc --p ./src/3-enums/tsconfig.json",
    "build4": "yarn clean && mkdir ./dist && tsc --p ./src/4-interfaces/tsconfig.json",
    "clean": "rm -rf ./dist"
  },
  "devDependencies": {
    "typescript": "^4.1.2"
  }
}
